
let attempt = 0;
while (attempt <= 10) {
    // console.log(`Attempt number: ${attempt}`);
    attempt += 2; // increment by 2 each time
}

let courses = ["Math", "CSE", "English"];
let i = 0;
while (i < courses.length) {
    // console.log(`Enrolled in: ${courses[i]}`);
    i++;
}

// Do-while loop: grades check
let grade = 11;
do {
    console.log(`Current grade: ${grade}`);
    grade++;
} while (grade <= 10); // this will run at least once
